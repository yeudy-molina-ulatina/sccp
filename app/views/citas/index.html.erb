<h1>Control de citas</h1>

<table>
  <tr>
    <th>Fecha</th>
    <th>Lugar atenci&oacute;n</th>
    <th>Hora</th>
    <th>Duraci&oacute;n</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @citas.each do |cita| %>
  <tr>
    <td><%= cita.fecha %></td>
    <td><%= cita.lugar_atencion %></td>
    <td><%= cita.hora %></td>
    <td><%= cita.duracion %></td>
    <td><%= link_to 'Mostrar', cita %></td>
    <td><%= link_to 'Editar', edit_cita_path(cita) %></td>
    <td><%= link_to 'Eliminar', cita, confirm: '¿Esta seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'Cita nueva', new_cita_path, :method => :get %>
